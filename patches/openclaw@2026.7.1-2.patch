diff --git a/dist/acp-cli-BXc5GttU.js b/dist/acp-cli-BXc5GttU.js
index 6c7de7d..e97f3e4 100644
--- a/dist/acp-cli-BXc5GttU.js
+++ b/dist/acp-cli-BXc5GttU.js
@@ -1622,6 +1622,40 @@ function buildSessionUsageSnapshot(row) {
 }
+function resolveAcpTextDelta(previousText, nextText) {
+	if (!nextText || nextText === previousText) return "";
+	if (!previousText) return nextText;
+	if (nextText.startsWith(previousText)) return nextText.slice(previousText.length);
+	if (previousText.startsWith(nextText)) return "";
+	return nextText;
+}
 //#endregion
 //#region src/acp/translator.replay.ts
+function extractToolResultReplay(message) {
+	const toolCallId = normalizeOptionalString(message.toolCallId);
+	if (!toolCallId) return [];
+	const rawOutput = {
+		content: message.content,
+		...message.details === void 0 ? {} : { details: message.details }
+	};
+	return [{
+		sessionUpdate: "tool_call_update",
+		toolCallId,
+		status: message.isError === true ? "failed" : "completed",
+		rawOutput,
+		content: extractToolCallContent(message.content) ?? extractToolCallContent(rawOutput),
+		locations: extractToolCallLocations(rawOutput)
+	}];
+}
 function extractReplayChunks(message) {
+	if (message.__openclaw?.kind === "compaction" && typeof message.__openclaw.id === "string" && message.__openclaw.id.trim()) return [{
+		sessionUpdate: "session_info_update",
+		_meta: { "openclaw.ai/compaction": {
+			version: 1,
+			compactionId: message.__openclaw.id,
+			status: "completed",
+			source: "transcript",
+			...timestampMsToIsoString(message.timestamp) ? { timestamp: timestampMsToIsoString(message.timestamp) } : {}
+		} }
+	}];
 	const role = typeof message.role === "string" ? message.role : "";
+	if (role === "toolResult") return extractToolResultReplay(message);
 	if (role !== "user" && role !== "assistant") return [];
@@ -1643,2 +1677,18 @@ function extractReplayChunks(message) {
 		}
+		if (role === "assistant" && typedBlock.type === "toolCall") {
+			const toolCallId = normalizeOptionalString(typedBlock.id);
+			const name = normalizeOptionalString(typedBlock.name);
+			if (!toolCallId) continue;
+			const args = asOptionalRecord(typedBlock.arguments);
+			replayChunks.push({
+				sessionUpdate: "tool_call",
+				toolCallId,
+				title: formatToolTitle(name, args),
+				status: "in_progress",
+				rawInput: args,
+				kind: inferToolKind(name),
+				locations: extractToolCallLocations(args)
+			});
+			continue;
+		}
 		if (role === "assistant" && typedBlock.type === "thinking" && typeof typedBlock.thinking === "string" && typedBlock.thinking) replayChunks.push({
@@ -1650,2 +1700,44 @@ function extractReplayChunks(message) {
 }
+function selectPostLedgerTranscript(transcript, ledgerReplay) { /* ledgerReplay.complete ? Promise.resolve([]) : this.getSessionTranscript was replaced so complete ledgers can append a bounded timestamp tail. */
+	let maxLedgerAt;
+	for (const event of ledgerReplay.events) {
+		const at = event?.at;
+		if (typeof at !== "number" || !Number.isFinite(at)) continue;
+		maxLedgerAt = maxLedgerAt === void 0 ? at : Math.max(maxLedgerAt, at);
+	}
+	const ledgerHighWaterAt = maxLedgerAt ?? Number.NEGATIVE_INFINITY;
+	return transcript.filter((message) => typeof message?.timestamp === "number" && Number.isFinite(message.timestamp) && message.timestamp > ledgerHighWaterAt);
+}
+const ACP_COMPACTION_SOURCES = /* @__PURE__ */ new Set([
+	"threshold",
+	"overflow",
+	"preflight",
+	"manual"
+]);
+const MAX_ACP_COMPACTION_REASON_CODE_CHARS = 100;
+const MAX_ACP_COMPACTION_REASON_CHARS = 500;
+function buildAcpCompactionUpdate(data, runId) {
+	const phase = data.phase;
+	const compactionId = normalizeOptionalString(data.compactionId);
+	const source = normalizeOptionalString(data.source) ?? normalizeOptionalString(data.reason);
+	if ((phase !== "start" && phase !== "end") || !compactionId || !source || !ACP_COMPACTION_SOURCES.has(source)) return;
+	const timestamp = normalizeOptionalString(data.timestamp) ?? timestampMsToIsoString(data.ts);
+	const status = phase === "start" ? "in_progress" : data.aborted === true ? "cancelled" : data.completed === true ? "completed" : "failed";
+	const reasonCode = status === "failed" ? normalizeOptionalString(data.reasonCode)?.slice(0, MAX_ACP_COMPACTION_REASON_CODE_CHARS) : void 0;
+	const reason = status === "failed" ? normalizeOptionalString(data.failureReason)?.slice(0, MAX_ACP_COMPACTION_REASON_CHARS) : void 0;
+	return {
+		sessionUpdate: "session_info_update",
+		_meta: { "openclaw.ai/compaction": {
+			version: 1,
+			compactionId,
+			status,
+			source,
+			...normalizeOptionalString(runId) ? { runId: normalizeOptionalString(runId) } : {},
+			...typeof data.willRetry === "boolean" ? { willRetry: data.willRetry } : {},
+			...timestamp ? { timestamp } : {},
+			...reasonCode ? { reasonCode } : {},
+			...reason ? { reason } : {}
+		} }
+	};
+}
 //#endregion
@@ -1851,2 +1943,4 @@ const ACP_LOAD_SESSION_REPLAY_LIMIT = 1e6;
 const ACP_GATEWAY_DISCONNECT_GRACE_MS = 5e3;
+// ClawX passes a policy-derived value covering heartbeat detection and managed Gateway startup.
+const ACP_GATEWAY_ACCEPTED_PROMPT_RECOVERY_GRACE_MS = Number.parseInt(process.env.OPENCLAW_ACP_ACCEPTED_PROMPT_RECOVERY_GRACE_MS ?? "", 10) || 6e5;
 function normalizedChatSendAckStatus(status) {
@@ -1916,2 +2010,5 @@ var AcpGatewayAgent = class {
 		this.pendingPrompts = /* @__PURE__ */ new Map();
+		this.sessionMessageSubscriptions = /* @__PURE__ */ new Map();
+		this.ambientSession = void 0;
+		this.ambientChatRuns = /* @__PURE__ */ new Map();
 		this.settlingPromptKeys = /* @__PURE__ */ new Set();
@@ -1943,4 +2040,5 @@ var AcpGatewayAgent = class {
-	shutdown() {
-		this.sessionUpdates.stop();
+	async shutdown() {
+		this.activeDisconnectContext = null;
+		this.clearDisconnectTimer();
+		await this.deactivateAmbientSession();
+		this.sessionUpdates.stop();
-		this.activeDisconnectContext = null;
-		this.clearDisconnectTimer();
@@ -1959,3 +2057,85 @@ var AcpGatewayAgent = class {
 	}
-	handleGatewayReconnect() {
+	async ensureSessionMessageSubscription(sessionKey, entry) {
+		if (entry.subscribed || entry.acquirePromise) return await entry.acquirePromise;
+		entry.acquirePromise = (async () => {
+			let finalError;
+			for (let attempt = 1; attempt <= 2; attempt++) try {
+				await this.gateway.request("sessions.messages.subscribe", { key: sessionKey });
+				if (this.sessionMessageSubscriptions.get(sessionKey) === entry && entry.refCount > 0) entry.subscribed = true;
+				else await this.gateway.request("sessions.messages.unsubscribe", { key: sessionKey }).catch((err) => {
+					this.log(`stale session message unsubscribe failed for ${sessionKey}: ${String(err)}`);
+				});
+				return;
+			} catch (err) {
+				finalError = err;
+				this.log(`session message subscription attempt ${attempt} failed for ${sessionKey}: ${String(err)}`);
+			}
+			throw finalError;
+		})();
+		try {
+			await entry.acquirePromise;
+		} finally {
+			entry.acquirePromise = void 0;
+		}
+	}
+	async acquireSessionMessageSubscription(sessionKey) {
+		let entry = this.sessionMessageSubscriptions.get(sessionKey);
+		if (!entry) {
+			entry = {
+				refCount: 0,
+				subscribed: false,
+				acquirePromise: void 0
+			};
+			this.sessionMessageSubscriptions.set(sessionKey, entry);
+		}
+		entry.refCount += 1;
+		try {
+			await this.ensureSessionMessageSubscription(sessionKey, entry);
+		} catch (err) {
+			entry.refCount = Math.max(0, entry.refCount - 1);
+			if (entry.refCount === 0 && this.sessionMessageSubscriptions.get(sessionKey) === entry) this.sessionMessageSubscriptions.delete(sessionKey);
+			throw err;
+		}
+	}
+	async releaseSessionMessageSubscription(sessionKey) {
+		const entry = this.sessionMessageSubscriptions.get(sessionKey);
+		if (!entry) return;
+		entry.refCount = Math.max(0, entry.refCount - 1);
+		if (entry.refCount > 0) return;
+		this.sessionMessageSubscriptions.delete(sessionKey);
+		if (entry.acquirePromise) try {
+			await entry.acquirePromise;
+		} catch (err) {
+			this.log(`session message subscription cleanup failed for ${sessionKey}: ${String(err)}`);
+		}
+		if (!entry.subscribed) return;
+		entry.subscribed = false;
+		await this.gateway.request("sessions.messages.unsubscribe", { key: sessionKey }).catch((err) => {
+			this.log(`session message unsubscribe failed for ${sessionKey}: ${String(err)}`);
+		});
+	}
+	async activateAmbientSession(session) {
+		const nextSession = {
+			sessionId: session.sessionId,
+			sessionKey: session.sessionKey,
+			...session.ledgerSessionId ? { ledgerSessionId: session.ledgerSessionId } : {}
+		};
+		const currentSession = this.ambientSession;
+		if (currentSession?.sessionId === nextSession.sessionId && currentSession.sessionKey === nextSession.sessionKey) {
+			this.ambientSession = nextSession;
+			return;
+		}
+		await this.acquireSessionMessageSubscription(nextSession.sessionKey);
+		this.ambientSession = nextSession;
+		this.ambientChatRuns.clear();
+		if (currentSession) await this.releaseSessionMessageSubscription(currentSession.sessionKey);
+	}
+	async deactivateAmbientSession(sessionId) {
+		const currentSession = this.ambientSession;
+		if (!currentSession || sessionId && currentSession.sessionId !== sessionId) return;
+		this.ambientSession = void 0;
+		this.ambientChatRuns.clear();
+		await this.releaseSessionMessageSubscription(currentSession.sessionKey);
+	}
+	async handleGatewayReconnect() {
 		this.log("gateway reconnected");
@@ -1963,4 +2143,18 @@ var AcpGatewayAgent = class {
 		this.activeDisconnectContext = null;
-		if (!disconnectContext) return;
-		this.reconcilePendingPrompts(disconnectContext.generation, false);
+		await Promise.all([...this.sessionMessageSubscriptions].map(async ([sessionKey, entry]) => {
+			if (entry.acquirePromise) try {
+				await entry.acquirePromise;
+			} catch (err) {
+				this.log(`session message subscription recovery failed for ${sessionKey}: ${String(err)}`);
+			}
+			entry.acquirePromise = void 0;
+			if (this.sessionMessageSubscriptions.get(sessionKey) !== entry || entry.refCount === 0) return;
+			entry.subscribed = false;
+			try {
+				await this.ensureSessionMessageSubscription(sessionKey, entry);
+			} catch (err) {
+				this.log(`session message subscription recovery failed for ${sessionKey}: ${String(err)}`);
+			}
+		}));
+		if (disconnectContext) await this.reconcilePendingPrompts(disconnectContext.generation, false);
 	}
@@ -1983,2 +2177,6 @@ var AcpGatewayAgent = class {
 		}
+		if (evt.event === "session.operation") {
+			await this.handleSessionOperationEvent(evt);
+			return;
+		}
 		if (evt.event === "exec.approval.requested") {
@@ -1987,3 +2185,3 @@ var AcpGatewayAgent = class {
 		}
-		if (evt.event === "agent") await this.handleAgentEvent(evt);
+		if (evt.event === "agent" || evt.event === "session.tool") await this.handleAgentEvent(evt);
 	}
@@ -2040 +2238,2 @@ var AcpGatewayAgent = class {
-		await this.sessionUpdates.sendAvailableCommands(session, { record: true });
+		await this.sessionUpdates.sendAvailableCommands(session, { record: true });
+		await this.activateAmbientSession(session);
@@ -2078 +2277 @@ var AcpGatewayAgent = class {
-		const [sessionSnapshot, transcript] = await Promise.all([this.getSessionSnapshot(session.sessionKey), ledgerReplay.complete ? Promise.resolve([]) : this.getSessionTranscript(session.sessionKey).catch((err) => {
+		const [sessionSnapshot, transcript] = await Promise.all([this.getSessionSnapshot(session.sessionKey), this.getSessionTranscript(session.sessionKey).catch((err) => {
@@ -2082,2 +2281,4 @@ var AcpGatewayAgent = class {
-		if (ledgerReplay.complete) await this.replayLedgerSession(session.sessionId, ledgerReplay);
-		else await this.replaySessionTranscript(session.sessionId, transcript);
+		if (ledgerReplay.complete) await this.replayLedgerSession(session.sessionId, ledgerReplay);
+		if (ledgerReplay.complete) await this.replaySessionTranscript(session.sessionId, selectPostLedgerTranscript(transcript, ledgerReplay));
+		else await this.replaySessionTranscript(session.sessionId, transcript);
+		await this.activateAmbientSession(session);
@@ -2150 +2351,2 @@ var AcpGatewayAgent = class {
-		await this.sessionUpdates.sendAvailableCommands(session, { record: false });
+		await this.sessionUpdates.sendAvailableCommands(session, { record: false });
+		await this.activateAmbientSession(session);
@@ -2160 +2362,2 @@ var AcpGatewayAgent = class {
-		await this.cancelSessionWork(session);
+		await this.cancelSessionWork(session);
+		await this.deactivateAmbientSession(session.sessionId);
@@ -2212,3 +2415,3 @@ var AcpGatewayAgent = class {
 		if (!session) throw new Error(`Session ${params.sessionId} not found`);
-		if (session.abortController) this.sessionStore.cancelActiveRun(params.sessionId);
+		if (session.abortController) await this.cancelSessionWork(session);
 		const meta = parseSessionMeta(params["_meta"]);
@@ -2229,3 +2432,2 @@ var AcpGatewayAgent = class {
 		const runId = randomUUID();
-		this.sessionStore.setActiveRun(params.sessionId, runId, abortController);
 		const requestParams = {
@@ -2239,2 +2441,4 @@ var AcpGatewayAgent = class {
 		};
+		await this.acquireSessionMessageSubscription(session.sessionKey);
+		this.sessionStore.setActiveRun(params.sessionId, runId, abortController);
 		return new Promise((resolve, reject) => {
@@ -2243,2 +2447,3 @@ var AcpGatewayAgent = class {
 				sessionKey: session.sessionKey,
+				subscriptionSessionKey: session.sessionKey,
 				...session.ledgerSessionId ? { ledgerSessionId: session.ledgerSessionId } : {},
@@ -2301,3 +2506,4 @@ var AcpGatewayAgent = class {
 				const promptKey = this.pendingPromptKey(params.sessionId, runId);
-				if (isGatewayCloseError(err) && (this.getPendingPrompt(params.sessionId, runId) || this.settlingPromptKeys.has(promptKey))) return;
+				const pending = this.pendingPrompts.get(params.sessionId);
+				if (isGatewayCloseError(err) && (this.getPendingPrompt(params.sessionId, runId) || pending?.resumedRunIds?.has(runId) || this.settlingPromptKeys.has(promptKey))) return;
 				this.clearApprovalRelaysForPrompt(params.sessionId, runId, { denyActive: true });
@@ -2305,2 +2511,3 @@ var AcpGatewayAgent = class {
 				this.sessionStore.clearActiveRun(params.sessionId);
+				void this.releaseSessionMessageSubscription(session.sessionKey);
 				if (this.pendingPrompts.size === 0) this.clearDisconnectTimer();
@@ -2338,2 +2545,8 @@ var AcpGatewayAgent = class {
 		if (!stream || !data || !sessionKey) return;
+		const pending = this.findPendingBySessionKey(sessionKey, runId, payload.resumedFromRunId);
+		if (stream === "compaction") {
+			if (!pending) return;
+			await this.emitCompactionUpdate(pending, data, runId);
+			return;
+		}
 		if (stream === "approval") {
@@ -2351,3 +2564,2 @@ var AcpGatewayAgent = class {
 		if (!toolCallId) return;
-		const pending = this.findPendingBySessionKey(sessionKey, runId);
 		if (!pending) return;
@@ -2425,2 +2637,24 @@ var AcpGatewayAgent = class {
 	}
+	async handleSessionOperationEvent(evt) {
+		const payload = evt.payload;
+		if (!payload || payload.operation !== "compact") return;
+		const sessionKey = normalizeOptionalString(payload.sessionKey);
+		if (!sessionKey) return;
+		const operationRunId = normalizeOptionalString(payload.runId);
+		const pending = operationRunId ? this.findPendingBySessionKey(sessionKey, operationRunId) : this.findUniquePendingBySessionKey(sessionKey);
+		if (!pending) return;
+		await this.emitCompactionUpdate(pending, payload, operationRunId);
+	}
+	async emitCompactionUpdate(pending, data, runId) {
+		const update = buildAcpCompactionUpdate(data, runId);
+		if (!update) return;
+		await this.sessionUpdates.emit({
+			sessionId: pending.sessionId,
+			sessionKey: pending.sessionKey,
+			...pending.ledgerSessionId ? { ledgerSessionId: pending.ledgerSessionId } : {},
+			runId: pending.idempotencyKey,
+			record: true,
+			update
+		});
+	}
 	async handleApprovalEvent(params) {
@@ -2444,2 +2678,4 @@ var AcpGatewayAgent = class {
 			sessionKey,
+			runId: normalizeOptionalString(request?.runId),
+			resumedFromRunId: normalizeOptionalString(request?.resumedFromRunId),
 			approvalEvent
@@ -2450,3 +2686,3 @@ var AcpGatewayAgent = class {
 		if (this.approvalRelays.has(approvalEvent.approvalId)) return;
-		const pending = params.runId ? this.findPendingBySessionKey(params.sessionKey, params.runId) : this.findUniquePendingBySessionKey(params.sessionKey);
+		const pending = params.runId ? this.findPendingBySessionKey(params.sessionKey, params.runId, params.resumedFromRunId) : this.findUniquePendingBySessionKey(params.sessionKey);
 		if (!pending) return;
@@ -2520 +2756,74 @@ var AcpGatewayAgent = class {
-	}
+	}
+	async handleAmbientChatEvent(session, runId, state, messageData) {
+		if (state !== "delta" && state !== "final" && state !== "aborted" && state !== "error") return;
+		let ambientRun = this.ambientChatRuns.get(runId);
+		if (ambientRun?.settled) return;
+		if (!ambientRun) {
+			if (this.ambientChatRuns.size >= 100) {
+				let evictionKey;
+				for (const [candidateKey, candidate] of this.ambientChatRuns) {
+					evictionKey ??= candidateKey;
+					if (candidate.settled) {
+						evictionKey = candidateKey;
+						break;
+					}
+				}
+				if (evictionKey) this.ambientChatRuns.delete(evictionKey);
+			}
+			ambientRun = { settled: false };
+			this.ambientChatRuns.set(runId, ambientRun);
+		}
+		const content = messageData?.content;
+		const fullThought = content?.filter((block) => block?.type === "thinking").map((block) => block.thinking ?? "").join("\n").trimEnd();
+		const newThought = resolveAcpTextDelta(ambientRun.sentThought ?? "", fullThought);
+		if (newThought) {
+			ambientRun.sentThought = fullThought;
+			await this.sessionUpdates.emit({
+				sessionId: session.sessionId,
+				sessionKey: session.sessionKey,
+				...session.ledgerSessionId ? { ledgerSessionId: session.ledgerSessionId } : {},
+				runId,
+				record: true,
+				update: {
+					sessionUpdate: "agent_thought_chunk",
+					content: {
+						type: "text",
+						text: newThought
+					}
+				}
+			});
+		}
+		const fullText = content?.filter((block) => block?.type === "text").map((block) => block.text ?? "").join("\n").trimEnd();
+		const newText = resolveAcpTextDelta(ambientRun.sentText ?? "", fullText);
+		if (newText) {
+			ambientRun.sentText = fullText;
+			await this.sessionUpdates.emit({
+				sessionId: session.sessionId,
+				sessionKey: session.sessionKey,
+				...session.ledgerSessionId ? { ledgerSessionId: session.ledgerSessionId } : {},
+				runId,
+				record: true,
+				update: {
+					sessionUpdate: "agent_message_chunk",
+					content: {
+						type: "text",
+						text: newText
+					}
+				}
+			});
+		}
+		if (state === "delta") return;
+		ambientRun.settled = true;
+		this.ambientChatRuns.delete(runId);
+		this.ambientChatRuns.set(runId, ambientRun);
+		try {
+			const sessionSnapshot = await this.getSessionSnapshot(session.sessionKey);
+			await this.sendSessionSnapshotUpdate(session, sessionSnapshot, {
+				includeControls: false,
+				record: true,
+				runId
+			});
+		} catch (err) {
+			this.log(`ambient session snapshot update failed for ${session.sessionId}: ${String(err)}`);
+		}
+	}
@@ -2528,5 +2837,10 @@ var AcpGatewayAgent = class {
 		if (!sessionKey || !state) return;
-		const pending = this.findPendingBySessionKey(sessionKey, runId);
+		const pending = this.findPendingBySessionKey(sessionKey, runId, payload.resumedFromRunId);
-		if (!pending) return;
+		if (!pending) {
+			const ambientSession = this.ambientSession;
+			if (!ambientSession || ambientSession.sessionKey !== sessionKey || typeof runId !== "string" || !runId.startsWith("announce:v1:")) return;
+			await this.handleAmbientChatEvent(ambientSession, runId, state, messageData);
+			return;
+		}
-		if (messageData && (state === "delta" || state === "final")) {
+		if (messageData && (state === "delta" || state === "final" || state === "aborted")) {
 			await this.handleDeltaEvent(pending.sessionId, messageData);
@@ -2553,5 +2867,4 @@ var AcpGatewayAgent = class {
 		const fullThought = content?.filter((block) => block?.type === "thinking").map((block) => block.thinking ?? "").join("\n").trimEnd();
-		const sentThoughtSoFar = pending.sentThoughtLength ?? 0;
-		if (fullThought && fullThought.length > sentThoughtSoFar) {
-			const newThought = fullThought.slice(sentThoughtSoFar);
+		const newThought = resolveAcpTextDelta(pending.sentThought ?? "", fullThought);
+		if (newThought) {
 			pending.sentThoughtLength = fullThought.length;
@@ -2574,5 +2887,4 @@ var AcpGatewayAgent = class {
 		const fullText = content?.filter((block) => block?.type === "text").map((block) => block.text ?? "").join("\n").trimEnd();
-		const sentSoFar = pending.sentTextLength ?? 0;
-		if (!fullText || fullText.length <= sentSoFar) return;
-		const newText = fullText.slice(sentSoFar);
+		const newText = resolveAcpTextDelta(pending.sentText ?? "", fullText);
+		if (!newText) return;
 		pending.sentTextLength = fullText.length;
@@ -2601,4 +2913,9 @@ var AcpGatewayAgent = class {
 			this.sessionStore.clearActiveRun(sessionId);
+			await this.releaseSessionMessageSubscription(pending.subscriptionSessionKey);
 			if (this.pendingPrompts.size === 0) this.clearDisconnectTimer();
-			const sessionSnapshot = await this.getSessionSnapshot(pending.sessionKey);
+			let sessionSnapshot = await this.getSessionSnapshot(pending.sessionKey);
+			if (!sessionSnapshot.usage || sessionSnapshot.usage.used === 0) {
+				await new Promise((resolve) => setTimeout(resolve, 1e3));
+				sessionSnapshot = await this.getSessionSnapshot(pending.sessionKey);
+			}
 			try {
@@ -2621,3 +2938,3 @@ var AcpGatewayAgent = class {
 	}
-	findPendingBySessionKey(sessionKey, runId) {
+	findPendingBySessionKey(sessionKey, runId, resumedFromRunId) {
 		for (const pending of this.pendingPrompts.values()) {
@@ -2627,2 +2944,8 @@ var AcpGatewayAgent = class {
 		}
+		if (runId && resumedFromRunId) for (const pending of this.pendingPrompts.values()) {
+			if (pending.idempotencyKey !== resumedFromRunId) continue;
+			this.reconcilePendingSessionKey(pending, sessionKey);
+			this.adoptResumedRun(pending, runId);
+			return pending;
+		}
 		if (runId) for (const pending of this.pendingPrompts.values()) {
@@ -2633,2 +2956,33 @@ var AcpGatewayAgent = class {
 	}
+	adoptResumedRun(pending, runId) {
+		const previousRunId = pending.idempotencyKey;
+		this.log(`prompt run resumed: ${previousRunId} -> ${runId}`);
+		this.clearApprovalRelaysForPrompt(pending.sessionId, previousRunId, { denyActive: true });
+		pending.resumedRunIds ??= /* @__PURE__ */ new Set();
+		pending.resumedRunIds.add(previousRunId);
+		pending.idempotencyKey = runId;
+		pending.sendAccepted = true;
+		pending.disconnectContext = void 0;
+		for (const [toolCallId, toolCall] of pending.toolCalls ?? []) this.sessionUpdates.emit({
+			sessionId: pending.sessionId,
+			sessionKey: pending.sessionKey,
+			...pending.ledgerSessionId ? { ledgerSessionId: pending.ledgerSessionId } : {},
+			runId: previousRunId,
+			record: true,
+			waitForDelivery: false,
+			update: {
+				sessionUpdate: "tool_call_update",
+				toolCallId,
+				status: "failed",
+				locations: toolCall.locations
+			}
+		});
+		pending.sentTextLength = 0;
+		pending.sentText = void 0;
+		pending.sentThoughtLength = 0;
+		pending.sentThought = void 0;
+		pending.toolCalls = void 0;
+		const session = this.sessionStore.getSession(pending.sessionId);
+		if (session?.abortController) this.sessionStore.setActiveRun(pending.sessionId, runId, session.abortController);
+	}
 	findUniquePendingBySessionKey(sessionKey) {
@@ -2654,3 +3008,3 @@ var AcpGatewayAgent = class {
 	}
-	armDisconnectTimer(disconnectContext) {
+	armDisconnectTimer(disconnectContext, deadline = "initial") {
 		this.clearDisconnectTimer();
@@ -2658,4 +3012,4 @@ var AcpGatewayAgent = class {
 			this.disconnectTimer = null;
-			this.reconcilePendingPrompts(disconnectContext.generation, true);
-		}, ACP_GATEWAY_DISCONNECT_GRACE_MS);
+			this.reconcilePendingPrompts(disconnectContext.generation, deadline);
+		}, deadline === "initial" ? ACP_GATEWAY_DISCONNECT_GRACE_MS : ACP_GATEWAY_ACCEPTED_PROMPT_RECOVERY_GRACE_MS - ACP_GATEWAY_DISCONNECT_GRACE_MS);
 		this.disconnectTimer.unref?.();
@@ -2667,2 +3021,3 @@ var AcpGatewayAgent = class {
 		this.sessionStore.clearActiveRun(pending.sessionId);
+		void this.releaseSessionMessageSubscription(pending.subscriptionSessionKey);
 		if (this.pendingPrompts.size === 0) this.clearDisconnectTimer();
@@ -2670,10 +3025,6 @@ var AcpGatewayAgent = class {
 	}
-	clearPendingDisconnectState(pending, disconnectContext) {
-		if (pending.disconnectContext !== disconnectContext) return;
-		pending.disconnectContext = void 0;
+	shouldRejectPendingAtDisconnectDeadline(pending, disconnectContext, deadline) {
+		return pending.disconnectContext === disconnectContext && (!pending.sendAccepted || deadline === "accepted-recovery");
 	}
-	shouldRejectPendingAtDisconnectDeadline(pending, disconnectContext) {
-		return pending.disconnectContext === disconnectContext && (!pending.sendAccepted || this.activeDisconnectContext?.generation === disconnectContext.generation);
-	}
-	async reconcilePendingPrompts(observedDisconnectGeneration, deadlineExpired) {
+	async reconcilePendingPrompts(observedDisconnectGeneration, deadline) {
 		if (this.pendingPrompts.size === 0) {
@@ -2687,9 +3038,13 @@ var AcpGatewayAgent = class {
 			if (pending.disconnectContext?.generation !== observedDisconnectGeneration) continue;
-			if (await this.reconcilePendingPrompt(sessionId, pending, deadlineExpired)) keepDisconnectTimer = true;
+			if (await this.reconcilePendingPrompt(sessionId, pending, deadline)) keepDisconnectTimer = true;
 		}
-		if (!keepDisconnectTimer && this.disconnectGeneration === observedDisconnectGeneration) this.clearDisconnectTimer();
+		if (keepDisconnectTimer && deadline === "initial" && this.disconnectGeneration === observedDisconnectGeneration) {
+			const disconnectContext = pendingEntries.map(([, pending]) => pending.disconnectContext).find((context) => context?.generation === observedDisconnectGeneration);
+			if (disconnectContext) this.armDisconnectTimer(disconnectContext, "accepted-recovery");
+		} else if (!keepDisconnectTimer && this.disconnectGeneration === observedDisconnectGeneration) this.clearDisconnectTimer();
 	}
-	async reconcilePendingPrompt(sessionId, pending, deadlineExpired) {
+	async reconcilePendingPrompt(sessionId, pending, deadline) {
 		const disconnectContext = pending.disconnectContext;
 		if (!disconnectContext) return false;
+		const waitedRunId = pending.idempotencyKey;
 		let result;
@@ -2697,3 +3052,4 @@ var AcpGatewayAgent = class {
 			result = await this.gateway.request("agent.wait", {
-				runId: pending.idempotencyKey,
+				runId: waitedRunId,
+				sessionKey: pending.sessionKey,
 				timeoutMs: 0
@@ -2701,5 +3057,5 @@ var AcpGatewayAgent = class {
 		} catch (err) {
-			this.log(`agent.wait reconcile failed for ${pending.idempotencyKey}: ${String(err)}`);
-			if (deadlineExpired) {
-				if (this.shouldRejectPendingAtDisconnectDeadline(pending, disconnectContext)) {
+			this.log(`agent.wait reconcile failed for ${waitedRunId}: ${String(err)}`);
+			if (deadline) {
+				if (this.shouldRejectPendingAtDisconnectDeadline(pending, disconnectContext, deadline)) {
 					this.rejectPendingPrompt(pending, /* @__PURE__ */ new Error(`Gateway disconnected: ${disconnectContext.reason}`));
@@ -2707,4 +3063,3 @@ var AcpGatewayAgent = class {
 				}
-				this.clearPendingDisconnectState(pending, disconnectContext);
-				return false;
+				return true;
 			}
@@ -2712,3 +3067,3 @@ var AcpGatewayAgent = class {
 		}
-		const currentPending = this.getPendingPrompt(sessionId, pending.idempotencyKey);
+		const currentPending = this.getPendingPrompt(sessionId, waitedRunId);
 		if (!currentPending) return false;
@@ -2722,4 +3077,4 @@ var AcpGatewayAgent = class {
 		}
-		if (deadlineExpired) {
-			if (this.shouldRejectPendingAtDisconnectDeadline(currentPending, disconnectContext)) {
+		if (deadline) {
+			if (this.shouldRejectPendingAtDisconnectDeadline(currentPending, disconnectContext, deadline)) {
 				const currentDisconnectContext = currentPending.disconnectContext;
@@ -2729,4 +3084,3 @@ var AcpGatewayAgent = class {
 			}
-			this.clearPendingDisconnectState(currentPending, disconnectContext);
-			return false;
+			return true;
 		}
@@ -2794,2 +3148,3 @@ var AcpGatewayAgent = class {
 			this.pendingPrompts.delete(session.sessionId);
+			await this.releaseSessionMessageSubscription(pending.subscriptionSessionKey);
 			if (this.pendingPrompts.size === 0) this.clearDisconnectTimer();
@@ -2892,3 +3247,3 @@ var AcpGatewayAgent = class {
 				sessionId,
-				update: {
+				update: chunk.sessionUpdate === "user_message_chunk" || chunk.sessionUpdate === "agent_message_chunk" || chunk.sessionUpdate === "agent_thought_chunk" ? {
 					sessionUpdate: chunk.sessionUpdate,
@@ -2898,3 +3253,3 @@ var AcpGatewayAgent = class {
 					}
-				}
+				} : chunk
 			});
@@ -3037,3 +3392,5 @@ async function serveAcpGateway(opts = {}) {
 			resolveGatewayReady();
-			agent?.handleGatewayReconnect();
+			void agent?.handleGatewayReconnect().catch((err) => {
+				process.stderr.write(`openclaw acp: gateway reconnect recovery failed: ${String(err)}\n`);
+			});
 		},
@@ -3055 +3412 @@ async function serveAcpGateway(opts = {}) {
-		activeAgent?.shutdown();
+		await activeAgent?.shutdown();
diff --git a/dist/agent-D6kiZtPt.js b/dist/agent-D6kiZtPt.js
index af55bc0..c2dff03 100644
--- a/dist/agent-D6kiZtPt.js
+++ b/dist/agent-D6kiZtPt.js
@@ -892,2 +892,20 @@ function yieldAfterAgentAcceptedAck() {
 }
+function resolvePersistedAgentWaitSnapshot(runId, sessionKey) {
+	if (!sessionKey) return;
+	try {
+		const entry = loadSessionEntry(sessionKey).entry;
+		if (normalizeOptionalString(entry?.lifecycleRunId) !== runId) return;
+		const status = entry?.status;
+		if (status !== "done" && status !== "failed" && status !== "timeout" && status !== "killed") return;
+		return {
+			runId,
+			status: status === "done" ? "ok" : "error",
+			startedAt: entry?.startedAt,
+			endedAt: entry?.endedAt,
+			...status === "done" ? {} : { error: `Agent run ended with session status ${status} during Gateway restart recovery` }
+		};
+	} catch {
+		return;
+	}
+}
 const agentHandlers = {
@@ -943,2 +961,11 @@ const agentHandlers = {
 		const inputProvenance = normalizeInputProvenance(request.inputProvenance);
+		const isRestartRecoveryResumeRun = canUseInternalRuntimeHandoff && inputProvenance?.kind === "internal_system" && inputProvenance.sourceTool === "main_session_restart_recovery";
+		if (request.internalRestartRecoverySourceRunId !== void 0 && !isRestartRecoveryResumeRun) {
+			respond(false, void 0, errorShape(ErrorCodes.INVALID_REQUEST, "restart recovery run lineage is reserved for main-session restart recovery."));
+			return;
+		}
+		if (request.internalRestartRecoverySourceRunId !== void 0 && request.internalRestartRecoverySourceRunId === runId) {
+			respond(false, void 0, errorShape(ErrorCodes.INVALID_REQUEST, "restart recovery must use a distinct run id from its source run."));
+			return;
+		}
 		const preserveUserFacingSessionModelState = canUseInternalRuntimeHandoff && shouldPreserveUserFacingSessionStateForInputProvenance(inputProvenance);
@@ -2194,3 +2221,4 @@ const agentHandlers = {
 					sessionKey: resolvedSessionKey,
-					lifecycleGeneration
+					lifecycleGeneration,
+					...request.internalRestartRecoverySourceRunId ? { resumedFromRunId: request.internalRestartRecoverySourceRunId } : {}
 				});
@@ -2504,2 +2532,7 @@ const agentHandlers = {
 		});
+		const persistedSnapshot = resolvePersistedAgentWaitSnapshot(runId, normalizeOptionalString(p.sessionKey));
+		if (persistedSnapshot) {
+			respond(true, persistedSnapshot);
+			return;
+		}
 		if (hasActiveChatRun) {
diff --git a/dist/agent-command-ABV9I5el.js b/dist/agent-command-ABV9I5el.js
index 7196652..ed78fb1 100644
--- a/dist/agent-command-ABV9I5el.js
+++ b/dist/agent-command-ABV9I5el.js
@@ -193,2 +193,3 @@ function createLocalGatewayRequestContext(params) {
 		getSessionEventSubscriberConnIds: () => sessionEvents,
+		getSessionMessageEventSubscriberConnIds: () => /* @__PURE__ */ new Set(),
 		registerToolEventRecipient: () => {},
diff --git a/dist/agent-events-CRggPZCM.js b/dist/agent-events-CRggPZCM.js
index d219ef4..9f6c72e 100644
--- a/dist/agent-events-CRggPZCM.js
+++ b/dist/agent-events-CRggPZCM.js
@@ -58,2 +58,3 @@ function registerAgentRunContext(runId, context) {
 	if (context.sessionId && existing.sessionId !== context.sessionId) existing.sessionId = context.sessionId;
+	if (context.resumedFromRunId && existing.resumedFromRunId !== context.resumedFromRunId) existing.resumedFromRunId = context.resumedFromRunId;
 	if (context.agentId && existing.agentId !== context.agentId) existing.agentId = context.agentId;
@@ -184,2 +185,3 @@ function enrichAgentEvent(event) {
 	const agentId = event.agentId ?? context?.agentId;
+	const resumedFromRunId = context?.resumedFromRunId;
 	const enriched = {
@@ -189,2 +191,3 @@ function enrichAgentEvent(event) {
 		...agentId ? { agentId } : {},
+		...resumedFromRunId ? { resumedFromRunId } : {},
 		seq: nextSeq,
diff --git a/dist/agent-harness-runtime-DUhcXa_t.d.ts b/dist/agent-harness-runtime-DUhcXa_t.d.ts
index 0d34d4b..3614665 100644
--- a/dist/agent-harness-runtime-DUhcXa_t.d.ts
+++ b/dist/agent-harness-runtime-DUhcXa_t.d.ts
@@ -1044,2 +1044,3 @@ type PreemptiveCompactionDecision = {
   toolResultReducibleChars: number;
+  toolResultAggregateTargetChars?: number;
   effectiveReserveTokens: number;
diff --git a/dist/agent-settings-axYuScuh.js b/dist/agent-settings-axYuScuh.js
index dec9ef8..cd75039 100644
--- a/dist/agent-settings-axYuScuh.js
+++ b/dist/agent-settings-axYuScuh.js
@@ -26,3 +26,3 @@ function applyAgentCompactionSettingsFromConfig(params) {
 	const configuredReserveTokens = toNonNegativeInt(compactionCfg?.reserveTokens);
-	const configuredKeepRecentTokens = toPositiveInt(compactionCfg?.keepRecentTokens);
+	const configuredKeepRecentTokens = toNonNegativeInt(compactionCfg?.keepRecentTokens);
 	let reserveTokensFloor = resolveCompactionReserveTokensFloor(params.cfg);
diff --git a/dist/agent-tools-BD8WL7ny.js b/dist/agent-tools-BD8WL7ny.js
index c16af06..75fe92b 100644
--- a/dist/agent-tools-BD8WL7ny.js
+++ b/dist/agent-tools-BD8WL7ny.js
@@ -1131,2 +1131,3 @@ function createOpenClawCodingTools(options) {
 		sessionKey: options?.sessionKey,
+		runId: options?.runId,
 		sessionId: options?.sessionId,
diff --git a/dist/attempt.tool-run-context-CuQsIXnb.js b/dist/attempt.tool-run-context-CuQsIXnb.js
index 37b39a3..bc82ef7 100644
--- a/dist/attempt.tool-run-context-CuQsIXnb.js
+++ b/dist/attempt.tool-run-context-CuQsIXnb.js
@@ -236,3 +236,3 @@ function shouldPreemptivelyCompactBeforePrompt(params) {
 	const overflowTokens = Math.max(0, estimatedPromptTokens - promptBudgetBeforeReserve);
-	const toolResultPotential = estimateToolResultReductionPotential({
+	const defaultToolResultPotential = estimateToolResultReductionPotential({
 		messages: messagesForPressure,
@@ -243,2 +243,9 @@ function shouldPreemptivelyCompactBeforePrompt(params) {
 	const truncateOnlyThresholdChars = Math.max(overflowChars + TRUNCATION_ROUTE_BUFFER_TOKENS * ESTIMATED_CHARS_PER_TOKEN, Math.ceil(overflowChars * 1.5));
+	const toolResultAggregateTargetChars = overflowTokens > 0 && defaultToolResultPotential.totalToolResultChars > 0 ? Math.max(1, Math.min(defaultToolResultPotential.aggregateBudgetChars, defaultToolResultPotential.totalToolResultChars - truncateOnlyThresholdChars)) : void 0;
+	const toolResultPotential = toolResultAggregateTargetChars === void 0 ? defaultToolResultPotential : estimateToolResultReductionPotential({
+		messages: messagesForPressure,
+		contextWindowTokens: params.contextTokenBudget,
+		maxCharsOverride: params.toolResultMaxChars,
+		aggregateMaxCharsOverride: toolResultAggregateTargetChars
+	});
 	const toolResultReducibleChars = toolResultPotential.maxReducibleChars;
@@ -256,2 +263,3 @@ function shouldPreemptivelyCompactBeforePrompt(params) {
 		toolResultReducibleChars,
+		...toolResultAggregateTargetChars !== void 0 ? { toolResultAggregateTargetChars } : {},
 		effectiveReserveTokens
diff --git a/dist/bash-tools-DHyGpWCr.js b/dist/bash-tools-DHyGpWCr.js
index def4eec..2ebd665 100644
--- a/dist/bash-tools-DHyGpWCr.js
+++ b/dist/bash-tools-DHyGpWCr.js
@@ -99,2 +99,5 @@ function buildExecApprovalRequestToolParams(params) {
 		sessionKey: params.sessionKey,
+		sessionId: params.sessionId,
+		runId: params.runId,
+		toolCallId: params.toolCallId,
 		turnSourceChannel: params.turnSourceChannel,
@@ -213,2 +216,5 @@ async function buildHostApprovalDecisionParams(params) {
 		}),
+		sessionId: params.sessionId,
+		runId: params.runId,
+		toolCallId: params.toolCallId,
 		resolvedPath: params.resolvedPath,
@@ -1145,2 +1151,5 @@ async function processGatewayAllowlist(params) {
 			}),
+			sessionId: params.sessionId,
+			runId: params.runId,
+			toolCallId: params.toolCallId,
 			approvalReviewerDeviceIds: params.approvalReviewerDeviceId ? [params.approvalReviewerDeviceId] : void 0,
@@ -1878,2 +1887,4 @@ async function executeNodeHostCommand(params) {
 		}),
+		runId: params.runId,
+		toolCallId: params.toolCallId,
 		approvalReviewerDeviceIds: params.approvalReviewerDeviceId ? [params.approvalReviewerDeviceId] : void 0,
@@ -3215,3 +3226,3 @@ function createExecTool(defaults) {
 		},
-		execute: async (_toolCallId, args, signal, onUpdate) => {
+		execute: async (toolCallId, args, signal, onUpdate) => {
 			let params = stripMalformedXmlArgValueSuffixFromKeys(args, XML_ARG_VALUE_EXEC_PARAM_KEYS);
@@ -3374,2 +3385,4 @@ function createExecTool(defaults) {
 					workdir,
+					toolCallId,
+					runId: defaults?.runId,
 					env,
@@ -3426,2 +3439,4 @@ function createExecTool(defaults) {
 						sessionKey: defaults?.sessionKey,
+						runId: defaults?.runId,
+						toolCallId,
 						sessionId: defaults?.sessionId,
diff --git a/dist/commands-handlers.runtime-CunY89Nu.js b/dist/commands-handlers.runtime-CunY89Nu.js
index aab709c..98aea8f 100644
--- a/dist/commands-handlers.runtime-CunY89Nu.js
+++ b/dist/commands-handlers.runtime-CunY89Nu.js
@@ -28,2 +28,3 @@ import { c as resolveContextConfigProviderForRuntime } from "./openai-routing-DX
 import { n as resolveAgentHarnessPolicy } from "./harness-runtimes-CA3PNIDt.js";
+import { c as emitAgentEvent } from "./agent-events-CRggPZCM.js";
 import "./archive-CBe_wA_B.js";
@@ -1456,4 +1457,24 @@ const handleCompactCommand = async (params) => {
 	});
-	const result = await runtime.compactEmbeddedAgentSession({
+	const runId = params.opts?.runId;
+	const compactionId = `cmp-${crypto.randomUUID()}`;
+	let result;
+	let compactError;
+	emitAgentEvent({
+		runId,
+		sessionKey: params.sessionKey,
+		stream: "compaction",
+		data: {
+			phase: "start",
+			compactionId,
+			source: "manual",
+			reason: "manual",
+			timestamp: new Date().toISOString(),
+			aborted: false,
+			completed: false
+		}
+	});
+	try {
+		result = await runtime.compactEmbeddedAgentSession({
 		abortSignal: params.opts?.abortSignal,
+		runId,
 		sessionId,
@@ -1492,3 +1513,30 @@ const handleCompactCommand = async (params) => {
 		ownerNumbers: params.command.ownerList.length > 0 ? params.command.ownerList : void 0
-	});
+		});
+	} catch (error) {
+		compactError = error;
+		throw error;
+	} finally {
+		const aborted = params.opts?.abortSignal?.aborted === true || compactError?.name === "AbortError" || result?.reason === "aborted";
+		const completed = result?.ok === true && result.compacted === true && !aborted;
+		const failureReason = !completed && !aborted ? compactError ? formatErrorMessage(compactError) : normalizeOptionalString(result?.reason) : void 0;
+		emitAgentEvent({
+			runId,
+			sessionKey: params.sessionKey,
+			stream: "compaction",
+			data: {
+				phase: "end",
+				compactionId,
+				source: "manual",
+				reason: "manual",
+				timestamp: new Date().toISOString(),
+				aborted,
+				completed,
+				willRetry: false,
+				...failureReason ? {
+					reasonCode: classifyCompactionReason(failureReason),
+					failureReason
+				} : {}
+			}
+		});
+	}
 	const compactLabel = result.ok || isCompactionSkipReason(result.reason) ? result.compacted ? result.result?.tokensBefore != null && result.result?.tokensAfter != null ? `Compacted (${runtime.formatTokenCount(result.result.tokensBefore)} → ${runtime.formatTokenCount(result.result.tokensAfter)})` : result.result?.tokensBefore ? `Compacted (${runtime.formatTokenCount(result.result.tokensBefore)} before)` : "Compacted" : "Compaction skipped" : "Compaction failed";
diff --git a/dist/compact-DLB4d8IL.js b/dist/compact-DLB4d8IL.js
index 49e5e86..4d7636a 100644
--- a/dist/compact-DLB4d8IL.js
+++ b/dist/compact-DLB4d8IL.js
@@ -1155,6 +1155,7 @@ async function compactEmbeddedAgentSessionDirectOnce(params) {
 					let transcriptRotationSessionManager = sessionManager;
-					if (params.trigger === "manual") try {
+					const configuredKeepRecentTokens = params.config?.agents?.defaults?.compaction?.keepRecentTokens;
+					if (params.trigger === "manual" || configuredKeepRecentTokens === 0) try {
 						const hardenedBoundary = await hardenManualCompactionBoundary({
 							sessionFile: params.sessionFile,
-							preserveRecentTail: typeof params.config?.agents?.defaults?.compaction?.keepRecentTokens === "number"
+							preserveRecentTail: typeof configuredKeepRecentTokens === "number" && configuredKeepRecentTokens > 0
 						});
@@ -1167,3 +1168,3 @@ async function compactEmbeddedAgentSessionDirectOnce(params) {
 					} catch (err) {
-						log.warn("[compaction] failed to harden manual compaction boundary", { errorMessage: formatErrorMessage(err) });
+						log.warn("[compaction] failed to harden compaction boundary", { errorMessage: formatErrorMessage(err) });
 					}
diff --git a/dist/embedded-agent-DGUuxGR2.js b/dist/embedded-agent-DGUuxGR2.js
index dc3b1af..b6c143b 100644
--- a/dist/embedded-agent-DGUuxGR2.js
+++ b/dist/embedded-agent-DGUuxGR2.js
@@ -19,3 +19,3 @@ import { i as getRuntimeConfigSnapshot } from "./runtime-snapshot-DOvWRYVz.js";
 import "./config-DbyjySSE.js";
-import { S as withAgentRunLifecycleGeneration, d as getAgentEventLifecycleGeneration, f as getAgentRunContext, g as registerAgentRunContext, l as emitAgentItemEvent, n as captureAgentRunLifecycleGeneration, r as claimAgentRunContext, t as assertAgentRunLifecycleGenerationCurrent } from "./agent-events-CRggPZCM.js";
+import { S as withAgentRunLifecycleGeneration, c as emitAgentEvent, d as getAgentEventLifecycleGeneration, f as getAgentRunContext, g as registerAgentRunContext, l as emitAgentItemEvent, n as captureAgentRunLifecycleGeneration, r as claimAgentRunContext, t as assertAgentRunLifecycleGenerationCurrent } from "./agent-events-CRggPZCM.js";
 import { t as getGlobalHookRunner } from "./hook-runner-global-BmIrGlLG.js";
@@ -93,3 +93,3 @@ import { t as ensureSelectedAgentHarnessPlugin } from "./runtime-plugin-BLmQLMt1
 import { t as ensureRuntimePluginsLoaded } from "./runtime-plugins-ICju_gEw.js";
-import { t as DEFERRED_CONTEXT_ENGINE_COMPACTION_REASON } from "./compact-reasons-ByJlIe1a.js";
+import { n as classifyCompactionReason, t as DEFERRED_CONTEXT_ENGINE_COMPACTION_REASON } from "./compact-reasons-ByJlIe1a.js";
 import { n as createAgentHarnessTaskRuntimeScope } from "./agent-harness-task-runtime-scope-AnHXK_PY.js";
@@ -2917,2 +2917,52 @@ async function runEmbeddedAgentInternal(paramsInput) {
 				};
+				const publishDirectCompactionEvent = async (data) => {
+					try {
+						emitAgentEvent({
+							runId: params.runId,
+							...params.sessionKey ? { sessionKey: params.sessionKey } : {},
+							stream: "compaction",
+							data
+						});
+						await params.onAgentEvent?.({
+							stream: "compaction",
+							data,
+							...params.sessionKey ? { sessionKey: params.sessionKey } : {}
+						});
+					} catch (error) {
+						log$1.debug(`direct compaction event callback failed: ${formatErrorMessage(error)}`);
+					}
+				};
+				const emitDirectCompactionStart = async (source) => {
+					const compactionId = `cmp-${Date.now().toString(36)}-${randomBytes(8).toString("hex")}`;
+					await publishDirectCompactionEvent({
+						phase: "start",
+						compactionId,
+						source,
+						reason: source,
+						timestamp: new Date().toISOString(),
+						aborted: false,
+						completed: false
+					});
+					return compactionId;
+				};
+				const emitDirectCompactionEnd = async (compactionId, source, compactResult) => {
+					if (!compactionId) return;
+					const aborted = params.abortSignal?.aborted === true || compactResult?.reason === "aborted";
+					const completed = compactResult.ok === true && compactResult.compacted === true && !aborted;
+					const failureReason = !completed && !aborted ? normalizeOptionalString(compactResult?.reason) : void 0;
+					await publishDirectCompactionEvent({
+						phase: "end",
+						compactionId,
+						source,
+						reason: source,
+						timestamp: new Date().toISOString(),
+						aborted,
+						completed,
+						willRetry: completed,
+						...failureReason ? {
+							reasonCode: classifyCompactionReason(failureReason),
+							failureReason
+						} : {}
+					});
+				};
 				const onCompactionHookMessages = async (payload) => {
@@ -3335,2 +3385,4 @@ async function runEmbeddedAgentInternal(paramsInput) {
 						const retryingFromTranscript = preflightRecovery.source === "mid-turn";
+						const isProgressContinuation = retryingFromTranscript && preflightRecovery.route === "truncate_tool_results_only" && preflightRecovery.truncatedCount === 0 && (attempt.toolMetas?.some((tool) => tool.isError !== true) ?? false);
+						if (isProgressContinuation) runLoopIterations = Math.max(0, runLoopIterations - 1);
 						log$1.info(`[context-overflow-precheck] early recovery route=${preflightRecovery.route} completed for ${provider}/${modelId}; ` + (retryingFromTranscript ? "retrying from current transcript" : "retrying prompt"));
@@ -3370,3 +3422,5 @@ async function runEmbeddedAgentInternal(paramsInput) {
 							await runOwnsCompactionBeforeHook("timeout recovery");
+							let timeoutCompactionId;
 							try {
+								timeoutCompactionId = await emitDirectCompactionStart("preflight");
 								const timeoutCompactionRuntimeContext = {
@@ -3428,2 +3482,4 @@ async function runEmbeddedAgentInternal(paramsInput) {
 								}, resolveCompactionTimeoutMs(params.config), params.abortSignal);
+								const previousSessionId = timeoutCompactResult.compacted ? adoptCompactionTranscript(timeoutCompactResult) : void 0;
+								await runOwnsCompactionAfterHook("timeout recovery", timeoutCompactResult, previousSessionId);
 							} catch (compactErr) {
@@ -3433,7 +3489,7 @@ async function runEmbeddedAgentInternal(paramsInput) {
 									compacted: false,
-									reason: String(compactErr)
+									reason: compactErr?.name === "AbortError" ? "aborted" : String(compactErr)
 								};
+							} finally {
+								await emitDirectCompactionEnd(timeoutCompactionId, "preflight", timeoutCompactResult);
 							}
-							const previousSessionId = timeoutCompactResult.compacted ? adoptCompactionTranscript(timeoutCompactResult) : void 0;
-							await runOwnsCompactionAfterHook("timeout recovery", timeoutCompactResult, previousSessionId);
 							if (timeoutCompactResult.compacted) {
@@ -3491,3 +3547,6 @@ async function runEmbeddedAgentInternal(paramsInput) {
 							await runOwnsCompactionBeforeHook("overflow recovery");
+							const directCompactionSource = promptErrorSource === "precheck" ? "preflight" : "overflow";
+							let directCompactionId;
 							try {
+								directCompactionId = await emitDirectCompactionStart(directCompactionSource);
 								const overflowCompactionRuntimeContext = {
@@ -3568,2 +3627,3 @@ async function runEmbeddedAgentInternal(paramsInput) {
 								}
+								await runOwnsCompactionAfterHook("overflow recovery", compactResult, previousSessionId);
 							} catch (compactErr) {
@@ -3573,17 +3633,44 @@ async function runEmbeddedAgentInternal(paramsInput) {
 									compacted: false,
-									reason: String(compactErr)
+									reason: compactErr?.name === "AbortError" ? "aborted" : String(compactErr)
 								};
+							} finally {
+								await emitDirectCompactionEnd(directCompactionId, directCompactionSource, compactResult);
 							}
-							await runOwnsCompactionAfterHook("overflow recovery", compactResult, previousSessionId);
 							if (preflightRecovery && isNoRealConversationCompactionNoop(compactResult)) {
-								lastCompactionTokensAfter = void 0;
-								lastContextBudgetStatus = void 0;
-								await resetNoRealConversationTokenSnapshot({
-									config: params.config,
-									sessionKey: params.sessionKey,
-									agentId: sessionAgentId
-								});
-								log$1.info(`[context-overflow-precheck] stale token state had no real conversation messages for ${provider}/${modelId}; resetting the context snapshot and retrying prompt`);
-								if (preflightRecovery.source === "mid-turn") continueFromCurrentTranscript();
-								continue;
+								const pressureProvesRealOverflow = preflightRecovery.overflowTokens > 0 && typeof preflightRecovery.pressureSource === "string" && preflightRecovery.pressureSource.length > 0;
+								if (preflightRecovery.toolResultAggregateTargetChars !== void 0) {
+									toolResultTruncationAttempted = true;
+									const truncationResult = await truncateOversizedToolResultsInSession({
+										sessionFile: activeSessionFile,
+										contextWindowTokens: ctxInfo.tokens,
+										maxCharsOverride: resolveLiveToolResultMaxChars({
+											contextWindowTokens: ctxInfo.tokens,
+											cfg: params.config,
+											agentId: sessionAgentId
+										}),
+										aggregateMaxCharsOverride: preflightRecovery.toolResultAggregateTargetChars,
+										sessionId: activeSessionId,
+										sessionKey: params.sessionKey,
+										agentId: sessionAgentId,
+										config: params.config,
+										protectTrailingToolResults: true
+									});
+									if (truncationResult.truncated) {
+										log$1.info(`[context-overflow-precheck] no-real-conversation compaction left tool-result pressure; truncated ${truncationResult.truncatedCount} tool result(s) and retrying prompt`);
+										if (preflightRecovery.source === "mid-turn") continueFromCurrentTranscript();
+										continue;
+									}
+									log$1.warn(`[context-overflow-precheck] pressure-derived tool-result truncation did not help after no-real-conversation compaction: ${truncationResult.reason ?? "unknown"}`);
+								} else if (!pressureProvesRealOverflow) {
+									lastCompactionTokensAfter = void 0;
+									lastContextBudgetStatus = void 0;
+									await resetNoRealConversationTokenSnapshot({
+										config: params.config,
+										sessionKey: params.sessionKey,
+										agentId: sessionAgentId
+									});
+									log$1.info(`[context-overflow-precheck] stale token state had no real conversation messages for ${provider}/${modelId}; resetting the context snapshot and retrying prompt`);
+									if (preflightRecovery.source === "mid-turn") continueFromCurrentTranscript();
+									continue;
+								} else log$1.warn(`[context-overflow-precheck] no-real-conversation compaction did not reduce measured prompt pressure; preserving the context snapshot`);
 							}
@@ -3601,2 +3688,3 @@ async function runEmbeddedAgentInternal(paramsInput) {
 										}),
+										aggregateMaxCharsOverride: preflightRecovery.toolResultAggregateTargetChars,
 										sessionId: activeSessionId,
@@ -3632,3 +3720,4 @@ async function runEmbeddedAgentInternal(paramsInput) {
 								contextWindowTokens,
-								maxCharsOverride: toolResultMaxChars
+								maxCharsOverride: toolResultMaxChars,
+								aggregateMaxCharsOverride: preflightRecovery?.toolResultAggregateTargetChars
 							}) : false) {
@@ -3640,2 +3729,3 @@ async function runEmbeddedAgentInternal(paramsInput) {
 									maxCharsOverride: toolResultMaxChars,
+									aggregateMaxCharsOverride: preflightRecovery?.toolResultAggregateTargetChars,
 									sessionId: activeSessionId,
diff --git a/dist/exec-approval-DRfKKxhu.js b/dist/exec-approval-DRfKKxhu.js
index f76d64d..eb42430 100644
--- a/dist/exec-approval-DRfKKxhu.js
+++ b/dist/exec-approval-DRfKKxhu.js
@@ -12,2 +12,3 @@ import { a as buildSystemRunApprovalEnvBinding, i as buildSystemRunApprovalBindi
 import { n as resolveSystemRunApprovalRequestContext } from "./system-run-approval-context-B9ONWoZl.js";
+import { f as getAgentRunContext } from "./agent-events-CRggPZCM.js";
 import { a as handleApprovalWaitDecision, c as listVisiblePendingApprovalRequests, f as resolvePendingApprovalRecord, i as handleApprovalResolve, l as registerPendingApprovalRecord, n as bindApprovalReviewerDeviceIds, o as handlePendingApprovalRequest, p as respondPendingApprovalLookupError, r as buildRequestedApprovalEvent, s as isApprovalRecordVisibleToClient, t as bindApprovalRequesterMetadata, u as resolveApprovalDecisionParams } from "./approval-shared-BKEyXMsJ.js";
@@ -227,2 +228,4 @@ function createExecApprovalHandlers(manager, opts) {
 			const unavailableDecisions = normalizeExecApprovalUnavailableDecisions(p.unavailableDecisions);
+			const requestRunId = normalizeOptionalString(p.runId);
+			const resumedFromRunId = requestRunId ? getAgentRunContext(requestRunId)?.resumedFromRunId : void 0;
 			const request = {
@@ -250,2 +253,6 @@ function createExecApprovalHandlers(manager, opts) {
 				sessionKey: effectiveSessionKey ?? null,
+				sessionId: normalizeOptionalString(p.sessionId) ?? null,
+				runId: requestRunId ?? null,
+				toolCallId: normalizeOptionalString(p.toolCallId) ?? null,
+				...resumedFromRunId ? { resumedFromRunId } : {},
 				turnSourceChannel: normalizeOptionalString(p.turnSourceChannel) ?? null,
diff --git a/dist/exec-approvals-bouecjdj.d.ts b/dist/exec-approvals-bouecjdj.d.ts
index c0a4168..71e438f 100644
--- a/dist/exec-approvals-bouecjdj.d.ts
+++ b/dist/exec-approvals-bouecjdj.d.ts
@@ -384,2 +384,6 @@ type ExecApprovalRequestPayload = {
   sessionKey?: string | null;
+  sessionId?: string | null;
+  runId?: string | null;
+  resumedFromRunId?: string;
+  toolCallId?: string | null;
   turnSourceChannel?: string | null;
diff --git a/dist/index-D5wkwzkn.d.ts b/dist/index-D5wkwzkn.d.ts
index 051154d..7989203 100644
--- a/dist/index-D5wkwzkn.d.ts
+++ b/dist/index-D5wkwzkn.d.ts
@@ -170,2 +170,3 @@ declare const validateAgentWaitParams: ProtocolValidator<{
   timeoutMs?: number | undefined;
+  sessionKey?: string | undefined;
   runId: string;
diff --git a/dist/main-session-restart-recovery-Ce8fihTV.js b/dist/main-session-restart-recovery-Ce8fihTV.js
index ba698af..c22cada 100644
--- a/dist/main-session-restart-recovery-Ce8fihTV.js
+++ b/dist/main-session-restart-recovery-Ce8fihTV.js
@@ -213,2 +213,5 @@ async function markStartupOrphanedMainSessionsForRecovery(params) {
 					})) continue;
+					const sourceRunId = normalizeOptionalString(entry.lifecycleRunId);
+					if (sourceRunId) entry.restartRecoveryDeliverySourceRunId = sourceRunId;
+					entry.restartRecoveryRuns = void 0;
 					entry.abortedLastRun = true;
@@ -352,2 +355,4 @@ async function resumeMainSession(params) {
 	const sanitizedPendingText = typeof params.pendingFinalDeliveryText === "string" ? sanitizePendingFinalDeliveryText(params.pendingFinalDeliveryText) : "";
+	const sourceRunIds = [...new Set((params.entry.restartRecoveryRuns ?? []).map((run) => typeof run.runId === "string" ? run.runId.trim() : "").filter(Boolean))];
+	const sourceRunId = sourceRunIds.length === 1 ? sourceRunIds[0] : sourceRunIds.length === 0 ? normalizeOptionalString(params.entry.restartRecoveryDeliverySourceRunId) : void 0;
 	const deliveryContext = resolveRestartRecoveryDeliveryContext({
@@ -361,5 +366,11 @@ async function resumeMainSession(params) {
 			sessionKey: params.sessionKey,
+			...sourceRunId ? { internalRestartRecoverySourceRunId: sourceRunId } : {},
 			idempotencyKey: crypto.randomUUID(),
 			deliver: Boolean(deliveryContext),
-			lane: "main"
+			lane: "main",
+			inputProvenance: {
+				kind: "internal_system",
+				sourceSessionKey: params.sessionKey,
+				sourceTool: "main_session_restart_recovery"
+			}
 		};
diff --git a/dist/proxy-BzhBz8iM.js b/dist/proxy-BzhBz8iM.js
index ca8bf1b..f33da4f 100644
--- a/dist/proxy-BzhBz8iM.js
+++ b/dist/proxy-BzhBz8iM.js
@@ -2440,3 +2440,8 @@ function prepareCompaction(pathEntries, settings) {
 	const tokensBefore = estimateContextTokens(buildSessionContext(pathEntries).messages).tokens;
-	const cutPoint = findCutPoint(pathEntries, boundaryStart, boundaryEnd, settings.keepRecentTokens);
+	const dropsCompletedTail = settings.keepRecentTokens === 0;
+	const cutPoint = dropsCompletedTail ? {
+		firstKeptEntryIndex: boundaryEnd - 1,
+		turnStartIndex: -1,
+		isSplitTurn: false
+	} : findCutPoint(pathEntries, boundaryStart, boundaryEnd, settings.keepRecentTokens);
 	const firstKeptEntry = pathEntries[cutPoint.firstKeptEntryIndex];
@@ -2444,3 +2449,3 @@ function prepareCompaction(pathEntries, settings) {
 	const firstKeptEntryId = firstKeptEntry.id;
-	const historyEnd = cutPoint.isSplitTurn ? cutPoint.turnStartIndex : cutPoint.firstKeptEntryIndex;
+	const historyEnd = dropsCompletedTail ? boundaryEnd : cutPoint.isSplitTurn ? cutPoint.turnStartIndex : cutPoint.firstKeptEntryIndex;
 	const messagesToSummarize = [];
diff --git a/dist/schema-BuOFpc7K.js b/dist/schema-BuOFpc7K.js
index 50b16b4..243b0b6 100644
--- a/dist/schema-BuOFpc7K.js
+++ b/dist/schema-BuOFpc7K.js
@@ -140,2 +140,3 @@ const AgentEventSchema = Type.Object({
 	runId: NonEmptyString,
+	resumedFromRunId: Type.Optional(NonEmptyString),
 	seq: Type.Integer({ minimum: 0 }),
@@ -282,2 +283,3 @@ const AgentParamsSchema = Type.Object({
 	internalRuntimeHandoffId: Type.Optional(NonEmptyString),
+	internalRestartRecoverySourceRunId: Type.Optional(NonEmptyString),
 	execApprovalFollowupExpectedSessionId: Type.Optional(NonEmptyString),
@@ -316,2 +318,3 @@ const AgentWaitParamsSchema = Type.Object({
 	runId: NonEmptyString,
+	sessionKey: Type.Optional(NonEmptyString),
 	timeoutMs: Type.Optional(Type.Integer({ minimum: 0 }))
@@ -2909,2 +2912,5 @@ const ExecApprovalRequestParamsSchema = Type.Object({
 	sessionKey: Type.Optional(Type.Union([Type.String(), Type.Null()])),
+	sessionId: Type.Optional(Type.Union([Type.String(), Type.Null()])),
+	runId: Type.Optional(Type.Union([Type.String(), Type.Null()])),
+	toolCallId: Type.Optional(Type.Union([Type.String(), Type.Null()])),
 	turnSourceChannel: Type.Optional(Type.Union([Type.String(), Type.Null()])),
@@ -3306,2 +3312,3 @@ const ChatEventBaseSchema = {
 	runId: NonEmptyString,
+	resumedFromRunId: Type.Optional(NonEmptyString),
 	sessionKey: NonEmptyString,
diff --git a/dist/schema-DtyqV_v0.d.ts b/dist/schema-DtyqV_v0.d.ts
index ff8cd28..be6988b 100644
--- a/dist/schema-DtyqV_v0.d.ts
+++ b/dist/schema-DtyqV_v0.d.ts
@@ -6,2 +6,3 @@ declare const AgentEventSchema: Type.TObject<{
   runId: Type.TString;
+  resumedFromRunId: Type.TOptional<Type.TString>;
   seq: Type.TInteger;
@@ -112,2 +113,3 @@ declare const AgentParamsSchema: Type.TObject<{
   internalRuntimeHandoffId: Type.TOptional<Type.TString>;
+  internalRestartRecoverySourceRunId: Type.TOptional<Type.TString>;
   execApprovalFollowupExpectedSessionId: Type.TOptional<Type.TString>;
@@ -3067,2 +3069,3 @@ declare const ProtocolSchemas: {
     internalRuntimeHandoffId: import("typebox").TOptional<import("typebox").TString>;
+    internalRestartRecoverySourceRunId: import("typebox").TOptional<import("typebox").TString>;
     execApprovalFollowupExpectedSessionId: import("typebox").TOptional<import("typebox").TString>;
@@ -3121,2 +3124,3 @@ declare const ProtocolSchemas: {
     runId: import("typebox").TString;
+    sessionKey: import("typebox").TOptional<import("typebox").TString>;
     timeoutMs: import("typebox").TOptional<import("typebox").TInteger>;
@@ -7575,2 +7579,5 @@ declare const ExecApprovalRequestParamsSchema: Type.TObject<{
   sessionKey: Type.TOptional<Type.TUnion<[Type.TString, Type.TNull]>>;
+  sessionId: Type.TOptional<Type.TUnion<[Type.TString, Type.TNull]>>;
+  runId: Type.TOptional<Type.TUnion<[Type.TString, Type.TNull]>>;
+  toolCallId: Type.TOptional<Type.TUnion<[Type.TString, Type.TNull]>>;
   turnSourceChannel: Type.TOptional<Type.TUnion<[Type.TString, Type.TNull]>>;
@@ -7869,2 +7876,3 @@ declare const ChatEventSchema: Type.TUnion<[Type.TObject<{
   runId: Type.TString;
+  resumedFromRunId: Type.TOptional<Type.TString>;
   sessionKey: Type.TString;
@@ -7879,2 +7887,3 @@ declare const ChatEventSchema: Type.TUnion<[Type.TObject<{
   runId: Type.TString;
+  resumedFromRunId: Type.TOptional<Type.TString>;
   sessionKey: Type.TString;
@@ -7889,2 +7898,3 @@ declare const ChatEventSchema: Type.TUnion<[Type.TObject<{
   runId: Type.TString;
+  resumedFromRunId: Type.TOptional<Type.TString>;
   sessionKey: Type.TString;
@@ -7901,2 +7911,3 @@ declare const ChatEventSchema: Type.TUnion<[Type.TObject<{
   runId: Type.TString;
+  resumedFromRunId: Type.TOptional<Type.TString>;
   sessionKey: Type.TString;
diff --git a/dist/selection-JInn13lc.js b/dist/selection-JInn13lc.js
index 41d3492..01f014e 100644
--- a/dist/selection-JInn13lc.js
+++ b/dist/selection-JInn13lc.js
@@ -3543,2 +3543,3 @@ function handleMessageUpdate(ctx, evt) {
 			ctx.emitAssistantStreamData(data, { emitPartialReply: true });
+			ctx.state.lastEmittedAssistantCleaned = cleanedText;
 			ctx.state.emittedAssistantUpdate = true;
@@ -3650,2 +3651,3 @@ function handleMessageEnd(ctx, evt) {
 		ctx.state.lastStreamedAssistant = void 0;
+		ctx.state.lastEmittedAssistantCleaned = void 0;
 		ctx.state.lastStreamedAssistantCleaned = void 0;
@@ -3653,3 +3655,3 @@ function handleMessageEnd(ctx, evt) {
 	};
-	const previousStreamedText = ctx.state.lastStreamedAssistantCleaned ?? "";
+	const previousStreamedText = ctx.state.lastEmittedAssistantCleaned ?? "";
 	const shouldReplaceFinalStream = Boolean(previousStreamedText && cleanedText && !cleanedText.startsWith(previousStreamedText));
@@ -3667,2 +3669,3 @@ function handleMessageEnd(ctx, evt) {
 		ctx.state.emittedAssistantUpdate = true;
+		ctx.state.lastEmittedAssistantCleaned = cleanedText;
 		ctx.state.lastStreamedAssistantCleaned = cleanedText;
@@ -3763,2 +3766,5 @@ function compactionLogKind(reason) {
 }
+function createCompactionId() {
+	return `cmp-${Date.now().toString(36)}-${generateSecureToken(8)}`;
+}
 /** Handles compaction start events from an embedded agent session. */
@@ -3767,2 +3773,5 @@ function handleCompactionStart(ctx, evt) {
 	const kind = compactionLogKind(reason);
+	const compactionId = createCompactionId();
+	const timestamp = new Date().toISOString();
+	ctx.state.activeCompactionId = compactionId;
 	ctx.state.compactionInFlight = true;
@@ -3778,4 +3787,13 @@ function handleCompactionStart(ctx, evt) {
 		runId: ctx.params.runId,
+		...ctx.params.sessionKey ? { sessionKey: ctx.params.sessionKey } : {},
 		stream: "compaction",
-		data: { phase: "start" }
+		data: {
+			phase: "start",
+			compactionId,
+			source: reason,
+			reason,
+			timestamp,
+			aborted: false,
+			completed: false
+		}
 	});
@@ -3783,3 +3801,12 @@ function handleCompactionStart(ctx, evt) {
 		stream: "compaction",
-		data: { phase: "start" }
+		...ctx.params.sessionKey ? { sessionKey: ctx.params.sessionKey } : {},
+		data: {
+			phase: "start",
+			compactionId,
+			source: reason,
+			reason,
+			timestamp,
+			aborted: false,
+			completed: false
+		}
 	});
@@ -3798,2 +3825,7 @@ function handleCompactionEnd(ctx, evt) {
 	const kind = compactionLogKind(reason);
+	const compactionId = ctx.state.activeCompactionId;
+	if (!compactionId) {
+		ctx.log.warn(`embedded run compaction end without matching start: runId=${ctx.params.runId} reason=${reason}`);
+		return;
+	}
 	ctx.state.compactionInFlight = false;
@@ -3802,2 +3834,4 @@ function handleCompactionEnd(ctx, evt) {
 	const wasAborted = Boolean(evt.aborted);
+	const timestamp = new Date().toISOString();
+	ctx.state.activeCompactionId = void 0;
 	if (hasResult && !wasAborted) {
@@ -3845,2 +3879,3 @@ function handleCompactionEnd(ctx, evt) {
 		runId: ctx.params.runId,
+		...ctx.params.sessionKey ? { sessionKey: ctx.params.sessionKey } : {},
 		stream: "compaction",
@@ -3848,4 +3883,9 @@ function handleCompactionEnd(ctx, evt) {
 			phase: "end",
+			compactionId,
+			source: reason,
+			reason,
+			timestamp,
 			willRetry,
-			completed: hasResult && !wasAborted
+			completed: hasResult && !wasAborted,
+			aborted: wasAborted
 		}
@@ -3854,6 +3894,12 @@ function handleCompactionEnd(ctx, evt) {
 		stream: "compaction",
+		...ctx.params.sessionKey ? { sessionKey: ctx.params.sessionKey } : {},
 		data: {
 			phase: "end",
+			compactionId,
+			source: reason,
+			reason,
+			timestamp,
 			willRetry,
-			completed: hasResult && !wasAborted
+			completed: hasResult && !wasAborted,
+			aborted: wasAborted
 		}
@@ -4331,2 +4377,3 @@ function subscribeEmbeddedAgentSession(params) {
 		lastStreamedAssistant: void 0,
+		lastEmittedAssistantCleaned: void 0,
 		lastStreamedAssistantCleaned: void 0,
@@ -4515,2 +4562,3 @@ function subscribeEmbeddedAgentSession(params) {
 		state.lastStreamedAssistant = void 0;
+		state.lastEmittedAssistantCleaned = void 0;
 		state.lastStreamedAssistantCleaned = void 0;
@@ -7428,2 +7476,3 @@ function toMidTurnPrecheckRequest(result) {
 		estimatedPromptTokens: result.estimatedPromptTokens,
+		pressureSource: result.pressureSource,
 		promptBudgetBeforeReserve: result.promptBudgetBeforeReserve,
@@ -7431,2 +7480,3 @@ function toMidTurnPrecheckRequest(result) {
 		toolResultReducibleChars: result.toolResultReducibleChars,
+		toolResultAggregateTargetChars: result.toolResultAggregateTargetChars,
 		effectiveReserveTokens: result.effectiveReserveTokens
@@ -11171,4 +11221,6 @@ function buildPreflightRecoveryBudgetSnapshot(snapshot) {
 		estimatedPromptTokens: snapshot.estimatedPromptTokens,
+		pressureSource: snapshot.pressureSource,
 		promptBudgetBeforeReserve: snapshot.promptBudgetBeforeReserve,
-		overflowTokens: snapshot.overflowTokens
+		overflowTokens: snapshot.overflowTokens,
+		toolResultAggregateTargetChars: snapshot.toolResultAggregateTargetChars
 	};
@@ -13583,2 +13635,3 @@ async function runEmbeddedAttempt(params) {
 						maxCharsOverride: toolResultMaxChars,
+						aggregateMaxCharsOverride: request.toolResultAggregateTargetChars,
 						sessionFile: params.sessionFile,
@@ -13598,2 +13651,11 @@ async function runEmbeddedAttempt(params) {
 						activeSession.agent.state.messages = sessionContext.messages;
+					} else if (truncationResult.reason === "no oversized or aggregate tool results") {
+						preflightRecovery = {
+							route: "truncate_tool_results_only",
+							source: "mid-turn",
+							...buildPreflightRecoveryBudgetSnapshot(request),
+							handled: true,
+							truncatedCount: 0
+						};
+						logMidTurnPrecheck(request.route, `handled=true truncatedCount=0 truncateSkippedReason=${truncationResult.reason}`);
 						logMidTurnPrecheck(request.route, `handled=true truncatedCount=${truncationResult.truncatedCount}`);
@@ -14190,2 +14252,3 @@ async function runEmbeddedAttempt(params) {
 							maxCharsOverride: toolResultMaxChars,
+							aggregateMaxCharsOverride: preemptiveCompaction.toolResultAggregateTargetChars,
 							sessionFile: params.sessionFile,
diff --git a/dist/server-chat-wgxNCdC3.js b/dist/server-chat-wgxNCdC3.js
index e03fece..59d43fe 100644
--- a/dist/server-chat-wgxNCdC3.js
+++ b/dist/server-chat-wgxNCdC3.js
@@ -546,6 +546,13 @@ function createAgentEventHandler({ broadcast, broadcastToConnIds, nodeSendToSess
 	const sendChatPayload = (sessionKey, payload, opts) => {
+		const payloadRecord = payload && typeof payload === "object" ? payload : void 0;
+		const runId = typeof payloadRecord?.runId === "string" ? payloadRecord.runId : void 0;
+		const resumedFromRunId = runId ? getAgentRunContext(runId)?.resumedFromRunId : void 0;
+		const projectedPayload = resumedFromRunId && payloadRecord ? {
+			...payloadRecord,
+			resumedFromRunId
+		} : payload;
 		const deliverySessionKey = resolveSessionDeliveryKey(sessionKey, opts?.agentId);
 		if (opts?.controlUiVisible ?? true) {
-			broadcast("chat", payload, { dropIfSlow: opts?.dropIfSlow });
-			sendNodeSessionPayloadForAgent(sessionKey, "chat", payload, opts?.agentId);
+			broadcast("chat", projectedPayload, { dropIfSlow: opts?.dropIfSlow });
+			sendNodeSessionPayloadForAgent(sessionKey, "chat", projectedPayload, opts?.agentId);
 			return;
@@ -553,3 +560,3 @@ function createAgentEventHandler({ broadcast, broadcastToConnIds, nodeSendToSess
 		const recipients = sessionMessageSubscribers.get(deliverySessionKey);
-		if (recipients.size > 0) broadcastToConnIds("chat", payload, recipients, { dropIfSlow: opts?.dropIfSlow });
+		if (recipients.size > 0) broadcastToConnIds("chat", projectedPayload, recipients, { dropIfSlow: opts?.dropIfSlow });
 	};
@@ -721,4 +728,7 @@ function createAgentEventHandler({ broadcast, broadcastToConnIds, nodeSendToSess
 		}
+		const restartsAfterLifecycleError = lifecyclePhase === "start" && pendingTerminalLifecycleErrors.has(evt.runId);
 		if (lifecyclePhase !== null && lifecyclePhase !== "error") clearPendingTerminalLifecycleError(evt.runId);
+		if (restartsAfterLifecycleError) clearBufferedChatState(clientRunId);
 		const spawnedBy = sessionKey ? resolveSpawnedBy(sessionKey) : null;
+		const resumedFromRunId = runContext?.resumedFromRunId;
 		const agentPayload = sessionKey ? {
@@ -728,6 +738,8 @@ function createAgentEventHandler({ broadcast, broadcastToConnIds, nodeSendToSess
 			...spawnedBy && { spawnedBy },
-			...isHeartbeat !== void 0 && { isHeartbeat }
+			...isHeartbeat !== void 0 && { isHeartbeat },
+			...resumedFromRunId ? { resumedFromRunId } : {}
 		} : {
 			...eventForClients,
-			...isHeartbeat !== void 0 && { isHeartbeat }
+			...isHeartbeat !== void 0 && { isHeartbeat },
+			...resumedFromRunId ? { resumedFromRunId } : {}
 		};
@@ -801,3 +813,6 @@ function createAgentEventHandler({ broadcast, broadcastToConnIds, nodeSendToSess
 			if (isControlUiVisible && sessionKey && !suppressHeartbeatToolEvents) {
-				const sessionSubscribers = excludeConnIds(sessionEventSubscribers.getAll(), runToolRecipients);
+				const sessionSubscribers = excludeConnIds(new Set([
+					...sessionEventSubscribers.getAll(),
+					...sessionMessageSubscribers.get(resolveSessionDeliveryKey(sessionKey, sessionAgentId))
+				]), runToolRecipients);
 				if (sessionSubscribers.size > 0) broadcastToConnIds("session.tool", {
@@ -848,3 +863,2 @@ function createAgentEventHandler({ broadcast, broadcastToConnIds, nodeSendToSess
 		if (lifecyclePhase === "error") {
-			clearBufferedChatState(clientRunId);
 			const skipChatErrorFinal = isChatSendRunActive(evt.runId) && !chatLink;
diff --git a/dist/server-request-context-CyBqotZW.js b/dist/server-request-context-CyBqotZW.js
index 0698d00..149da6c 100644
--- a/dist/server-request-context-CyBqotZW.js
+++ b/dist/server-request-context-CyBqotZW.js
@@ -98,2 +98,3 @@ function createGatewayRequestContext(params) {
 		getSessionEventSubscriberConnIds: params.getSessionEventSubscriberConnIds,
+		getSessionMessageEventSubscriberConnIds: params.getSessionMessageEventSubscriberConnIds,
 		registerToolEventRecipient: params.registerToolEventRecipient,
diff --git a/dist/server.impl-qYPVZMND.js b/dist/server.impl-qYPVZMND.js
index 59ed810..1b7023f 100644
--- a/dist/server.impl-qYPVZMND.js
+++ b/dist/server.impl-qYPVZMND.js
@@ -2529,2 +2529,3 @@ async function startGatewayServer(port = 18789, opts = {}) {
 				getSessionEventSubscriberConnIds: sessionEventSubscribers.getAll,
+				getSessionMessageEventSubscriberConnIds: sessionMessageSubscribers.get,
 				registerToolEventRecipient: toolEventRecipients.add,
diff --git a/dist/session-lifecycle-state-Czgc8l0p.js b/dist/session-lifecycle-state-Czgc8l0p.js
index db68c12..43f833d 100644
--- a/dist/session-lifecycle-state-Czgc8l0p.js
+++ b/dist/session-lifecycle-state-Czgc8l0p.js
@@ -99,6 +99,14 @@ function derivePersistedSessionLifecyclePatch(params) {
 	}
-	return patch;
+	const phase = resolveLifecyclePhase(params.event);
+	return {
+		...patch,
+		...phase === "start" ? { lifecycleRunId: runId } : {}
+	};
 }
 function deriveGatewaySessionLifecycleProjectionPatch(params) {
-	const { restartRecoveryRuns: _restartRecoveryRuns, ...patch } = derivePersistedSessionLifecyclePatch(params);
+	const {
+		restartRecoveryRuns: _restartRecoveryRuns,
+		lifecycleRunId: _lifecycleRunId,
+		...patch
+	} = derivePersistedSessionLifecyclePatch(params);
 	return patch;
diff --git a/dist/sessions-UcKjjh_n.js b/dist/sessions-UcKjjh_n.js
index e07a744..66559be 100644
--- a/dist/sessions-UcKjjh_n.js
+++ b/dist/sessions-UcKjjh_n.js
@@ -35,2 +35,3 @@ import { r as resolveIngressWorkspaceOverrideForSpawnedRun } from "./spawned-con
 import { r as compactEmbeddedAgentSession } from "./embedded-agent-DGUuxGR2.js";
+import { n as classifyCompactionReason } from "./compact-reasons-ByJlIe1a.js";
 import { a as isSessionLifecycleMutationActive, i as interruptSessionWorkAdmissions, o as isSessionWorkAdmissionActive, s as runExclusiveSessionLifecycleMutation, t as SESSION_WORK_ADMISSION_DRAIN_TIMEOUT_MS } from "./session-lifecycle-admission-DfdITEs1.js";
@@ -122,3 +123,9 @@ function shouldAttachPendingMessageSeq(params) {
 function emitSessionOperation(context, payload) {
-	const connIds = context.getSessionEventSubscriberConnIds();
+	const connIds = new Set(context.getSessionEventSubscriberConnIds());
+	const subscriptionKey = resolveSessionMessageSubscriptionKey({
+		canonicalKey: payload.sessionKey,
+		agentId: payload.agentId,
+		defaultAgentId: resolveDefaultAgentId(context.getRuntimeConfig())
+	});
+	for (const connId of context.getSessionMessageEventSubscriberConnIds(subscriptionKey)) connIds.add(connId);
 	if (connIds.size === 0) return;
@@ -1875,4 +1882,6 @@ const sessionsHandlers = {
 					const operationId = randomUUID();
+					const compactionId = randomUUID();
 					emitSessionOperation(context, {
 						operationId,
+						compactionId,
 						operation: "compact",
@@ -1880,13 +1889,32 @@ const sessionsHandlers = {
 						sessionKey: target.canonicalKey,
+						source: "manual",
+						reason: "manual",
+						timestamp: new Date().toISOString(),
+						aborted: false,
+						completed: false,
 						...target.canonicalKey === "global" && target.agentId ? { agentId: target.agentId } : {}
 					});
-					const emitCompactionEnd = (completed, reason) => emitSessionOperation(context, {
-						operationId,
-						operation: "compact",
-						phase: "end",
-						sessionKey: target.canonicalKey,
-						...target.canonicalKey === "global" && target.agentId ? { agentId: target.agentId } : {},
-						completed,
-						reason
-					});
+					const emitCompactionEnd = (completed, detail) => {
+						const detailText = detail == null ? void 0 : formatErrorMessage(detail);
+						const aborted = !completed && (detail instanceof Error && detail.name === "AbortError" || detailText?.toLowerCase() === "aborted" || detailText?.toLowerCase() === "operation aborted");
+						emitSessionOperation(context, {
+							operationId,
+							compactionId,
+							operation: "compact",
+							phase: "end",
+							sessionKey: target.canonicalKey,
+							...target.canonicalKey === "global" && target.agentId ? { agentId: target.agentId } : {},
+							source: "manual",
+							reason: "manual",
+							timestamp: new Date().toISOString(),
+							aborted,
+							completed: completed && !aborted,
+							willRetry: false,
+							...!completed && !aborted && detailText ? {
+								reasonCode: classifyCompactionReason(detailText),
+								failureReason: detailText
+							} : {},
+							detail: detailText
+						});
+					};
 					let result;
@@ -1916,3 +1944,3 @@ const sessionsHandlers = {
 					} catch (err) {
-						emitCompactionEnd(false, formatErrorMessage(err));
+						emitCompactionEnd(false, err);
 						throw err;
@@ -1949,3 +1977,3 @@ const sessionsHandlers = {
 						} catch (err) {
-							emitCompactionEnd(false, formatErrorMessage(err));
+							emitCompactionEnd(false, err);
 							throw err;
diff --git a/dist/store-BJJhlPrk.js b/dist/store-BJJhlPrk.js
index 15b9c3b..e62c2a2 100644
--- a/dist/store-BJJhlPrk.js
+++ b/dist/store-BJJhlPrk.js
@@ -149,2 +149,3 @@ const SESSION_ENTRY_RESERVED_SLOT_KEYS = /* @__PURE__ */ new Set([
 	"sessionId",
+	"lifecycleRunId",
 	"lifecycleRevision",
@@ -231,2 +232,3 @@ const SESSION_ENTRY_RESERVED_SLOT_KEYS = /* @__PURE__ */ new Set([
 	"restartRecoveryDeliveryRunId",
+	"restartRecoveryDeliverySourceRunId",
 	"totalTokensFresh",
diff --git a/dist/tool-result-truncation-rzMcqvvu.js b/dist/tool-result-truncation-rzMcqvvu.js
index d6ebea2..6524e62 100644
--- a/dist/tool-result-truncation-rzMcqvvu.js
+++ b/dist/tool-result-truncation-rzMcqvvu.js
@@ -446,3 +446,4 @@ function buildAggregateToolResultReplacements(params) {
 		...aggregateRecoveryCandidates.filter((item) => !item.aggregateEligible),
-		...candidates.filter((item) => item.deferredByFreshProjection && !item.protectedByTrailingBatch)
+		...candidates.filter((item) => item.deferredByFreshProjection && !item.protectedByTrailingBatch),
+		...candidates.filter((item) => item.protectedByTrailingBatch)
 	];
diff --git a/dist/types-DVCyjomt.d.ts b/dist/types-DVCyjomt.d.ts
index 41fa129..ef8c9ba 100644
--- a/dist/types-DVCyjomt.d.ts
+++ b/dist/types-DVCyjomt.d.ts
@@ -199,2 +199,4 @@ type SessionEntry = {
   systemSent?: boolean;
+  lifecycleRunId?: string;
+  restartRecoveryDeliverySourceRunId?: string;
   abortedLastRun?: boolean; /** Interrupted run generations whose late lifecycle events must be ignored. */
diff --git a/dist/zod-schema-O9ml_nmo.js b/dist/zod-schema-O9ml_nmo.js
index e7e2a95..a87dc28 100644
--- a/dist/zod-schema-O9ml_nmo.js
+++ b/dist/zod-schema-O9ml_nmo.js
@@ -204,3 +204,3 @@ const AgentDefaultsSchema = object({
 		reserveTokens: number().int().nonnegative().optional(),
-		keepRecentTokens: number().int().positive().optional(),
+		keepRecentTokens: number().int().nonnegative().optional(),
 		reserveTokensFloor: number().int().nonnegative().optional(),
diff --git a/dist/io-By0s-a_s.js b/dist/io-By0s-a_s.js
--- a/dist/io-By0s-a_s.js
+++ b/dist/io-By0s-a_s.js
@@ -989,9 +989,11 @@ function resolveConfigObserveSuspiciousReasons(params) {
 	const reasons = [];
 	const baseline = params.lastKnownGood;
 	if (!baseline) return reasons;
-	if (baseline.bytes >= 512 && params.bytes < Math.floor(baseline.bytes * .5)) reasons.push(`size-drop-vs-last-good:${baseline.bytes}->${params.bytes}`);
+	const baselineBytes = baseline.canonicalBytes ?? baseline.bytes;
+	const currentBytes = params.canonicalBytes ?? params.bytes;
+	if (baselineBytes >= 512 && currentBytes < Math.floor(baselineBytes * .5)) reasons.push(`size-drop-vs-last-good:${baseline.bytes}->${params.bytes}`);
 	if (baseline.hasMeta && !params.hasMeta) reasons.push("missing-meta-vs-last-good");
 	if (baseline.gatewayMode && !params.gatewayMode) reasons.push("gateway-mode-missing-vs-last-good");
 	if (baseline.gatewayMode && isUpdateChannelOnlyRoot(params.parsed)) reasons.push("update-channel-only-root");
 	return reasons;
 }
@@ -4736,6 +4738,10 @@ function setConfigHealthEntry(state, configPath, entry) {
 		}
 	};
 }
+function resolveCanonicalConfigBytes(raw, parsed) {
+	if (!isRecord$1(parsed)) return Buffer.byteLength(raw, "utf-8");
+	return Buffer.byteLength(JSON.stringify(parsed, null, 2).trimEnd().concat("\n"), "utf-8");
+}
 async function readConfigFingerprintForPath(deps, targetPath) {
 	try {
 		const raw = await deps.fs.promises.readFile(targetPath, "utf-8");
@@ -4744,6 +4750,7 @@ async function readConfigFingerprintForPath(deps, targetPath) {
 		return {
 			hash: hashConfigRaw(raw),
 			bytes: Buffer.byteLength(raw, "utf-8"),
+			canonicalBytes: resolveCanonicalConfigBytes(raw, parsed),
 			mtimeMs: stat?.mtimeMs ?? null,
 			ctimeMs: stat?.ctimeMs ?? null,
 			...resolveConfigStatMetadata(stat),
@@ -4764,6 +4771,7 @@ function readConfigFingerprintForPathSync(deps, targetPath) {
 		return {
 			hash: hashConfigRaw(raw),
 			bytes: Buffer.byteLength(raw, "utf-8"),
+			canonicalBytes: resolveCanonicalConfigBytes(raw, parsed),
 			mtimeMs: stat?.mtimeMs ?? null,
 			ctimeMs: stat?.ctimeMs ?? null,
 			...resolveConfigStatMetadata(stat),
@@ -4781,4 +4789,4 @@ function sameFingerprint(left, right) {
-	if (!left) return false;
-	return left.hash === right.hash && left.bytes === right.bytes && left.mtimeMs === right.mtimeMs && left.ctimeMs === right.ctimeMs && left.dev === right.dev && left.ino === right.ino && left.mode === right.mode && left.nlink === right.nlink && left.uid === right.uid && left.gid === right.gid && left.hasMeta === right.hasMeta && left.gatewayMode === right.gatewayMode;
+	if (!left) return false;
+	return left.hash === right.hash && left.bytes === right.bytes && left.canonicalBytes === right.canonicalBytes && left.mtimeMs === right.mtimeMs && left.ctimeMs === right.ctimeMs && left.dev === right.dev && left.ino === right.ino && left.mode === right.mode && left.nlink === right.nlink && left.uid === right.uid && left.gid === right.gid && left.hasMeta === right.hasMeta && left.gatewayMode === right.gatewayMode;
 }
 async function observeConfigSnapshot(deps, snapshot) {
@@ -4790,6 +4798,7 @@ async function observeConfigSnapshot(deps, snapshot) {
 	const current = {
 		hash: resolveConfigSnapshotHash(snapshot) ?? hashConfigRaw(snapshot.raw),
 		bytes: Buffer.byteLength(snapshot.raw, "utf-8"),
+		canonicalBytes: resolveCanonicalConfigBytes(snapshot.raw, snapshot.parsed),
 		mtimeMs: stat?.mtimeMs ?? null,
 		ctimeMs: stat?.ctimeMs ?? null,
 		...resolveConfigStatMetadata(stat),
@@ -4800,9 +4809,11 @@ async function observeConfigSnapshot(deps, snapshot) {
 	};
 	let healthState = await readConfigHealthState(deps);
 	const entry = getConfigHealthEntry(healthState, snapshot.path);
-	const backupBaseline = entry.lastKnownGood ?? await readConfigFingerprintForPath(deps, `${snapshot.path}.bak`) ?? void 0;
+	const backupFingerprint = await readConfigFingerprintForPath(deps, `${snapshot.path}.bak`);
+	const backupBaseline = backupFingerprint?.hash === entry.lastKnownGood?.hash ? backupFingerprint : entry.lastKnownGood ?? backupFingerprint ?? void 0;
 	const suspicious = resolveConfigObserveSuspiciousReasons({
 		bytes: current.bytes,
+		canonicalBytes: current.canonicalBytes,
 		hasMeta: current.hasMeta,
 		gatewayMode: current.gatewayMode,
 		parsed: snapshot.parsed,
@@ -4893,6 +4904,7 @@ function observeConfigSnapshotSync(deps, snapshot) {
 	const current = {
 		hash: resolveConfigSnapshotHash(snapshot) ?? hashConfigRaw(snapshot.raw),
 		bytes: Buffer.byteLength(snapshot.raw, "utf-8"),
+		canonicalBytes: resolveCanonicalConfigBytes(snapshot.raw, snapshot.parsed),
 		mtimeMs: stat?.mtimeMs ?? null,
 		ctimeMs: stat?.ctimeMs ?? null,
 		...resolveConfigStatMetadata(stat),
@@ -4903,9 +4915,11 @@ function observeConfigSnapshotSync(deps, snapshot) {
 	};
 	let healthState = readConfigHealthStateSync(deps);
 	const entry = getConfigHealthEntry(healthState, snapshot.path);
-	const backupBaseline = entry.lastKnownGood ?? readConfigFingerprintForPathSync(deps, `${snapshot.path}.bak`) ?? void 0;
+	const backupFingerprint = readConfigFingerprintForPathSync(deps, `${snapshot.path}.bak`);
+	const backupBaseline = backupFingerprint?.hash === entry.lastKnownGood?.hash ? backupFingerprint : entry.lastKnownGood ?? backupFingerprint ?? void 0;
 	const suspicious = resolveConfigObserveSuspiciousReasons({
 		bytes: current.bytes,
+		canonicalBytes: current.canonicalBytes,
 		hasMeta: current.hasMeta,
 		gatewayMode: current.gatewayMode,
 		parsed: snapshot.parsed,
